# Spark (HelpSpot) API Configuration
# Copy this file to .env and fill in your actual credentials
# IMPORTANT: Never commit .env to git (it's already in .gitignore)

HELPSPOT_URL=https://zap.caseshare.com/spark
HELPSPOT_USERNAME=your-username@example.com
HELPSPOT_PASSWORD=your-password-here

# Optional: Use API token instead of username/password
# HELPSPOT_API_TOKEN=your-api-token-here

# Optional: Disable SSL verification (not recommended for production)
# Set this to any value to disable SSL verification
# HELPSPOT_NO_VERIFY_SSL=1
